engines:
  duplication:
    enabled: true
    config:
        languages:
        - java
  checkstyle:
    enabled: true
    channel: beta
    config: '.checkstyle.xml'
  pmd:
    enabled: true
    channel: beta
ratings:
  paths:
  # Check only production files
  - 'injector/src/main/java/**'
  - 'injector-extras/src/main/java/**'

